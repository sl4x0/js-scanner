# v4.0 "Stealth Network" Dependencies
# Core async I/O
aiofiles>=23.2.1
uvloop>=0.19.0; sys_platform != 'win32'

# v4.0 Critical: Stealth networking (WAF bypass)
curl_cffi>=0.5.10

# Browser automation
playwright>=1.40.0

# Configuration
PyYAML>=6.0.1

# Code processing
jsbeautifier>=1.14.11
tree-sitter>=0.20.4,<0.23.0
tree-sitter-javascript>=0.20.0

# UI/Terminal
colorama>=0.4.6

# Performance optimization (optional)
# Install for Bloom filter support: pip install pybloom-live
pybloom-live>=1.0.3

# Legacy (for compatibility)
requests>=2.31.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0
pytest-cov>=4.0.0
